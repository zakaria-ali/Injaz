name: (disabled) Jekyll GitHub Pages

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Jekyll GitHub Pages workflow is disabled in favor of Vite deploy."
